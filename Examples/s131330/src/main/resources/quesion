Возник вопрос отоносительно токена для бота ТГ
Не могу создать файл без имени с раширением ".telegram"
Бота удается завести, если в этой строчке:

public BotClient() {
        this(Configuration.loadKeyFrom(".telegram"), ClientFactory.createOkClient());
    }

добавить ".telegram.txt" расширение.

Я что-то упустил или это опечатка?